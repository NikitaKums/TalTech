@model CategoryCreateViewModel

@{
    ViewData["Title"] = @Resources.Views.Common.Edit;
}

<h1>@Resources.Views.Common.Edit @Resources.Domain.Category.Header</h1>
<hr />
<div>
    <div class="col-md-6">
        <div class="card card-success">
            <div class="card-header">
                <h3 class="card-title">@Resources.Views.Common.Edit @Resources.Domain.Category.Header</h3>
            </div>
            <form asp-action="Edit">
                <div class="card-body">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="Category.CategoryName" class="control-label"></label>
                        <input asp-for="Category.CategoryName" class="form-control"/>
                        <span asp-validation-for="Category.CategoryName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Category.Shop" class="control-label"></label>
                        <select asp-for="Category.ShopId" class="form-control select-auto-complete" asp-items="Model.ShopSelectList"></select>
                        <span asp-validation-for="Category.CategoryName" class="text-danger"></span>
                    </div>
                    <input type="hidden" asp-for="Category.Id"/>
                    <button type="submit" class="btn bg-success float-right">@Resources.Views.Common.Edit <i class="nav-icon fa fa-pencil"></i></button>
                    <a asp-action="Index" class="btn btn-default">@Resources.Views.Common.Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
